#ifndef __COMMON__
#define __COMMON__

#include <stdbool.h>

char* concat(char* s1, char* s2);
char* clonestr(char* s);
bool sameString(char* s1, char* s2, int nbCharToCompare);

#endif /* __COMMON__ */
